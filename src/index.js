import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

//Trigger the App component to be rendered to the DOM
ReactDOM.render(<App />, document.getElementById('root'));